int board[3][3]; // initialise the board with zeros

for (int i = 0 ; i <= 2; i++)
{
    for (int j = 0; j <= 2; j++)
    {
        board[i][j] = 0;  // initialise the board
    }
}

if (board[0][0]== board[0][1] && board[0][0] == board[0][2])

